# Python
__pycache__/
*.py[cod]
.venv/
dbenv/
venv/

# Pipeline cache (large DB pull — never commit)
*.pkl
raw_snapshot.pkl

# Credentials / secrets
.env
*.creds
get_creds.py
# Flags backend SAS token (deployed to blob only, never committed)
flags-config.js

# OS
.DS_Store
